---
name: MARA-model-init-config
description: Use when the user wants to generate a default model routing config through `MARA model init-config`.
version: 1.0.0
---

# MARA Model Init Config

## Scope

Use this skill to create a default model routing config through the `MARA` product CLI.

## Command

- `MARA model init-config --output modelcli.yml`

## Focus

Use this before provider checks when no model routing config exists.
