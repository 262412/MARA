# Azure App Service and other pip-only deployment paths use this file as the
# standard dependency entrypoint. Keep dependency details in the package
# pyproject.toml files; these editable entries only wire the local workspace.
# Pin transitive dependencies so pip does not spend hours backtracking during
# GitHub Actions and Azure Oryx installs.
-c constraints.txt
-e ./libs/kotaemon
-e ./libs/ktem
-e ./libs/slide_cli
