---
description: List saved top-level slide sessions
argument-hint: [sessions args]
allowed-tools: Bash(slide:*)
---

List saved top-level slide sessions.

If `slide` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`slide sessions $ARGUMENTS`
