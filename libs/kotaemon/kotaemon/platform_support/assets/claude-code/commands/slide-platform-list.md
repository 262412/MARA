---
description: List supported AI coding platform bundles through slide
argument-hint: ""
allowed-tools: Bash(slide:*)
---

List supported platform bundles.

If `slide` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`slide platform list`

