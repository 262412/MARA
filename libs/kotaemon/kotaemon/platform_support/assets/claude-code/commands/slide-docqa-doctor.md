---
description: Validate slide DocQA runtime readiness
argument-hint: [doctor args]
allowed-tools: Bash(slide:*)
---

Validate slide DocQA runtime readiness.

If `slide` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`slide docqa doctor $ARGUMENTS`
