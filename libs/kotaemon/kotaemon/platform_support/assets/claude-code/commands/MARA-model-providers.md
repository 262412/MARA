---
description: Inspect model routing provider availability through MARA
argument-hint: [--config modelcli.yml]
allowed-tools: Bash(MARA:*)
---

Inspect model routing provider availability.

If `MARA` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`MARA model providers $ARGUMENTS`
