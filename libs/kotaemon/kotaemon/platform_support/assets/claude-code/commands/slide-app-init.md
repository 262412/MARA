---
description: Initialize packaged app user config through slide
argument-hint: [init args]
allowed-tools: Bash(slide:*)
---

Initialize packaged app user config.

If `slide` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`slide app init $ARGUMENTS`
