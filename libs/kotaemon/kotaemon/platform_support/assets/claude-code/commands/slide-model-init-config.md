---
description: Generate a default model routing config through slide
argument-hint: --output modelcli.yml
allowed-tools: Bash(slide:*)
---

Generate a default model routing config.

If `slide` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`slide model init-config $ARGUMENTS`

