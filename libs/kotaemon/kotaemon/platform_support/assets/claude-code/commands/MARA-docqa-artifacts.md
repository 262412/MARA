---
description: Manage generated MARA DocQA study artifacts
argument-hint: [artifacts args]
allowed-tools: Bash(MARA:*)
---

Manage generated MARA DocQA study artifacts.

If `MARA` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`MARA docqa artifacts $ARGUMENTS`
