---
description: Inspect installed Codex or Claude Code support assets through MARA
argument-hint: --platform <codex|claude-code> [--target-dir path]
allowed-tools: Bash(MARA:*)
---

Inspect installed platform support assets.

If `MARA` is not available, install the standalone CLI first:

- `pip install mara-research-cli`
- or `uv tool install mara-research-cli`

Run:
!`MARA platform status $ARGUMENTS`
