---
description: Manage MARA DocQA selected sources
argument-hint: [sources args]
allowed-tools: Bash(MARA:*)
---

Manage selected sources for a MARA DocQA notebook conversation.

If `MARA` is not available, install the standalone CLI first:

- `pip install mara-research-cli`
- or `uv tool install mara-research-cli`

Run:
!`MARA docqa sources $ARGUMENTS`
