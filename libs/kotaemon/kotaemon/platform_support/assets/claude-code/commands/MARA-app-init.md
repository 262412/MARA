---
description: Initialize packaged app user config through MARA
argument-hint: [init args]
allowed-tools: Bash(MARA:*)
---

Initialize packaged app user config.

If `MARA` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`MARA app init $ARGUMENTS`
