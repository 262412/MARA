---
description: Generate a default model routing config through MARA
argument-hint: --output modelcli.yml
allowed-tools: Bash(MARA:*)
---

Generate a default model routing config.

If `MARA` is not available, install the standalone CLI first:

- `pip install mara-research-cli`
- or `uv tool install mara-research-cli`

Run:
!`MARA model init-config $ARGUMENTS`
