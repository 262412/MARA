---
description: Inspect packaged app runtime health through MARA
argument-hint: [doctor args]
allowed-tools: Bash(MARA:*)
---

Inspect packaged app runtime health.

If `MARA` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`MARA app doctor $ARGUMENTS`
