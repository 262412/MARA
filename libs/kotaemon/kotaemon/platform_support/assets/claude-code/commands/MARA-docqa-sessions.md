---
description: List saved MARA DocQA conversations
argument-hint: [sessions args]
allowed-tools: Bash(MARA:*)
---

List saved MARA DocQA conversations.

If `MARA` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`MARA docqa sessions $ARGUMENTS`
