---
description: Inspect model routing provider availability through slide
argument-hint: [--config modelcli.yml]
allowed-tools: Bash(slide:*)
---

Inspect model routing provider availability.

If `slide` is not available, install the standalone CLI first:

- `pip install slide-cli`
- or `uv tool install slide-cli`

Run:
!`slide model providers $ARGUMENTS`
