---
name: MARA-model-providers
description: Use when the user wants to inspect model routing provider availability through `MARA model providers`.
version: 1.0.0
---

# MARA Model Providers

## Scope

Use this skill to inspect configured provider availability through the `MARA` product CLI.

## Command

- `MARA model providers --config modelcli.yml`

## Focus

Run this before real model calls, especially after config or secret changes.
