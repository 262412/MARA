---
name: slide-model-providers
description: Use when the user wants to inspect model routing provider availability through `slide model providers`.
version: 1.0.0
---

# Slide Model Providers

## Scope

Use this skill to inspect configured provider availability through the `slide` product CLI.

## Command

- `slide model providers --config modelcli.yml`

## Focus

Run this before real model calls, especially after config or secret changes.

